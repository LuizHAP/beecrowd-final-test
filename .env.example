# Server
PORT=3000
NODE_ENV=development

# Database (PostgreSQL via Docker Compose — port 5433 to avoid local conflicts)
DATABASE_URL=postgresql://ecommerce:ecommerce123@127.0.0.1:5433/ecommerce

# LLM / AI Agent (optional — deterministic mode works without these)
OPENAI_API_KEY=sk-...
LLM_MODE=deterministic
